// ==============================|| APP CUSTOMIZATION ||============================== //
// Central place for all application customization variables.
// Add any future customization options (logo, colors, footer text, etc.) here.

const customization = {
  // Application title (used for the browser tab / document title and the logo text)
  title: 'Eggshell Admin',

  // Application logo file (relative to the `public` directory, no subdirectories)
  // Change to 'logo.png' or another file type if needed — the extension is not enforced.
  logo: 'logo.svg'
};

export default customization;
